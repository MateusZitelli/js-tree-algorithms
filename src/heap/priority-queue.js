'use strict';

/**
 * Implements set S of elements, each of
 * elements associated with a key.
 */

/**
 * Inserts an element into the set
 * @param {x}
 */
function insert () {
};

/**
 * Retrieves an element of the set with the
 * largest key.
 */
function max () {

};

/**
 * retrieves an element from the set with the
 * largest key and removes it from the set.
 */
function extractMax () {

}

/**
 * Increases key (the priority) of a given element 'x' from the set.
 * @param  {Object} x
 * @param  {number} k
 */
function increaseKey (x, k) {
  // body...
}

function PQMax () {
  // body...
}

module.exports = {

};
